export const metadata = { title: 'QA Shield 🛡️', description: 'Automated QA lifecycle platform' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
